"""Local Web UI (see docs/WEB-UI.md). FastAPI + Jinja2 + vanilla JS,
runs on the same Windows machine as everything else in this project --
no cloud, no external services, no auth beyond "on your LAN".
"""
