__pycache__/
*.py[cod]
*.egg-info/

.pytest_cache/
.venv/
.idea/

# Runtime state -- regenerated locally, not source (real SQLite db incl.
# WAL/SHM, per-job work dirs, the inbox/ drop folder, manual mtp-probe
# write-test output).
data/
work/
inbox/
logs/
runtime.json
update_check_cache.json
tools/_write_test_artifacts/

mock_switch/

# PyInstaller / Inno Setup build output -- not source, rebuilt by CI and
# by `pyinstaller packaging/SwitchAgent.spec` / `iscc packaging/SwitchAgent.iss`.
build/
dist/
.local-build/
*.spec.bak
packaging/output/
*.exe
*.zip
SHA256SUMS.txt

# Launch/marketing drafts -- kept local on purpose, not part of the product.
promo/

# Local-only promo/marketing working material, not part of the product
promo/
screenshots/Gemini_Generated_Image_*

# Full-size screen recording; the committed demo is SwitchAgent-demo.gif
screenshots/SwitchAgent.gif
